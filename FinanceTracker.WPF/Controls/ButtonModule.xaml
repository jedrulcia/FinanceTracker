<UserControl x:Class="FinanceTracker.WPF.Controls.ButtonModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinanceTracker.WPF.Controls"
             xmlns:controls="clr-namespace:FinanceTracker.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Border Background="{StaticResource BrushSecondaryBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25*"/>
                <RowDefinition Height="25*"/>
                <RowDefinition Height="25*"/>
                <RowDefinition Height="25*"/>
            </Grid.RowDefinitions>


            <!-- SELECT LIST MONTH -->
            <Grid Grid.Row="0">
                <controls:ComboBoxNavigator/>
            </Grid>

            <!-- SALARY CREATE BUTTON -->
            <Grid Grid.Row="1">
                <controls:CreateButton/>
            </Grid>

            <!-- ONGOING EXPENSE CREATE BUTTON -->
            <Grid Grid.Row="2">
                <controls:CreateButton/>
            </Grid>

            <!-- OTHER EXPENSE CREATE BUTTON -->
            <Grid Grid.Row="3">
                <controls:CreateButton/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
